import LoginForm from '../../components/LoginForm';
import './style.scss';

const LoginPage = () => {
  return (
    <div className='LoginPage'>
      <h2>로그인</h2>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
