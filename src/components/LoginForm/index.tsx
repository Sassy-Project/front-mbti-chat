import Button from '../styled-components/Button';
import './style.scss';

const LoginForm = () => {
  return (
    <div className='LoginForm'>
      <form>이메일</form>
      <Button />
    </div>
  );
};

export default LoginForm;
